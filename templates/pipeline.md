# pipeline.md — Internal ledger (runtime; gitignored)

The status + sequence ledger TRON keeps when the host has no pipeline doc of its own. Seeded by interview (see `tron-seed.md` Step 5). TRON updates statuses as blocks progress; it is authoritative during a session. Joins to specs by ID.

Status ∈ `todo` · `in-progress` · `blocked` · `review` · `done`.

| Order | ID | Owner | Status | Notes |
|:------|:---|:------|:-------|:------|
|       |    |       |        |       |
